# github (Hatch)

**Portable as-is:** no. Auth seam: Hatch `dynamic_credentials.py` / `hsurr:*` /
`hatch_gws_cli`. Foreign stack: `gh` or GitHub App. Import SKILL.md from the
Hatch VM with `ops/skills/import_skills.py`.
