# Versioned custom skills

Canonical tree for #580 E7. See `AUTH_SEAM.md`.

| Dir | Origin |
|-----|--------|
| `grok-build/` | `~/.grok/skills/` (this Mac, 2026-09-11) |
| `hatch/` | Hatch `~/workspace/skills/` — import when that home is reachable |
