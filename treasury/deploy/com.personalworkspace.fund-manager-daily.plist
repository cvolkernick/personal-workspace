<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  RETIRED (#729). Pi is the fund-manager producer (fund-manager.timer).
  Do not bootstrap this LaunchAgent — it dual-runs the team/LLM path and,
  on a stale Mac checkout, still POSTs ntfy.sh (#704 missed this host).

  Unload if still present:
    launchctl bootout gui/$(id -u)/com.personalworkspace.fund-manager-daily 2>/dev/null || true
    launchctl disable gui/$(id -u)/com.personalworkspace.fund-manager-daily 2>/dev/null || true
    rm -f ~/Library/LaunchAgents/com.personalworkspace.fund-manager-daily.plist

  See treasury/deploy/FUND_MANAGER_PRODUCER.md
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.personalworkspace.fund-manager-daily</string>
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/true</string>
  </array>
  <key>RunAtLoad</key>
  <false/>
  <key>Disabled</key>
  <true/>
</dict>
</plist>
