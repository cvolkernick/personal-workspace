# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Logs / temp
*.log
tmp/
temp/
*.tmp
test-results/

# Orchestra heartbeat runtime (collector artifact; keep .gitkeep)
orchestra/data/heartbeat/latest.json
orchestra/data/heartbeat/.*.tmp.*

# Domain day-constraint snapshots (refresh writers; Orchestra reads only)
ops/board/day_constraints.json
ops/board/.day_constraints.*.tmp
fitness/data/day_constraints.json
fitness/data/.day_constraints.*.tmp

# Local Playwright install (scratch / accidental)
**/node_modules/
playwright-report/

# The command center server is local only
# (no need to ignore the launchers, they are useful)
dashboard/current-branch.txt
dashboard/branches.txt

# Add more as needed for your projects

# Resistance dashboard local secrets (if ever placed under repo)
resistance-dashboard/.env
resistance-dashboard/.env.local

# Full Grok session archives must never land in this repo (use ~/Backups/)
Backups/
**/grok-sessions-*.tar.gz
