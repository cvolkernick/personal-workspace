# IoT

Home automation bits (currently Wiz lights for the entryway).

**Visual dashboard:** See the IoT section in [../dashboard/index.html](../dashboard/index.html).

- `wiz-lights/` — Python CLI + bulbs.json (path now repo-relative)